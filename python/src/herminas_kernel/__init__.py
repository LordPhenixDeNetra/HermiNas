"""HermiNas Python kernel (L0): domain contracts, error taxonomy, path
layout, RBAC roles and immutable settings for the intelligence layer.
Mirrors (conceptually, not by shared code) kernel/ (Go) and
rust/kernel/src (Rust) until the protobuf contracts land in M0.3.
"""
